extends templatefile.pug 

block title
    title Account

block stylesheet 
    style(type="text/css").
        @import url('https://fonts.googleapis.com/css2?family=Lilita+One&family=Rubik+Mono+One&display=swap');
        form {
            margin: auto;
            text-align: center;
        }
        .text{
            margin-top: 100px;
        }

block content
    div(class="content")
        div(class="header")
            h1= "Welcome back, " + username + "!"
        div(class="text")
            h2 Sign Out
            form(action="/sign-out" method="get")
                input(type="submit" id="sign-out" name="sign-out")